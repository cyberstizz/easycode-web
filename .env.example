# Leave VITE_API_BASE_URL empty in local dev — vite.config.js proxies /v1 to :8080.
VITE_API_BASE_URL=
# Set to "1" to run the whole app off src/lib/mock.js with no backend at all.
VITE_USE_MOCK=1
VITE_STRIPE_PUBLISHABLE_KEY=
